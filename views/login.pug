block content
  form(method='POST' action='/log-in')
    .form-group 
      label(for='username') Username
      input#username.form-control(type='text' name='username' placeholder='Digite seu usuário')
    .form-group
      label(for='password') Password
      input#password.form-control(type='password' name='password' placeholder='Digite sua senha')
    button.submit(type='submit') Log In
    a(href='/sign-up') or Sign Up
  if errors
    ul
      for error in errors
        li.errormsg!= error.msg